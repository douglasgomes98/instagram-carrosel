export type Slide = {
  id: number;
  type: "cover";
  kicker: string;
  title: string;
  footer: string;
  image?: string;
};

export const carousel = {
  slug: "novo-carrossel",
  title: "Novo carrossel",
  format: "1080 × 1350",
};
