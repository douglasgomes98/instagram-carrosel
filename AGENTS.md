# Project instructions

For any task involving UI, frontend components, layouts, styles, or visual assets, use the `project-design` skill and read `DESIGN.md` before making design decisions. Treat `DESIGN.md` as the project's visual source of truth unless the user explicitly requests a deviation.
